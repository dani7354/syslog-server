version: "3.9"
services:
  ssh-tunnel:
      image: ssh-tunnel
      build:
        context: ./ssh-tunnel
        dockerfile: Dockerfile
      volumes:
        - ".ssh:/opt/ssh_tunnel/.ssh:ro"
      networks:
        - localnetwork
  rsyslog:
      image: rsyslog
      build:
        context: ./rsyslog
        dockerfile: Dockerfile
      volumes:
        - "./log:/var/log/remote"
      ports:
        - 514:514
      networks:
        - localnetwork
networks:
  localnetwork:
